project Spellcheck is

   for Source_Dirs use (".");
   for Exec_Dir use ".";
   for Object_Dir use "./obj";
   for Main use ("spellcheck");

   package Builder is
      for Default_Switches ("Ada") use ("-gnat05");
   end Builder;

end Spellcheck;
