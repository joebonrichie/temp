project Anagram is

   for Main use ("anagram", "anagram2");
   for Source_Dirs use (".");
   for Object_Dir use "./obj";
   for Exec_Dir use ".";

   package Builder is
      for Default_Switches ("Ada") use ("-gnat05");
   end Builder;

end Anagram;
