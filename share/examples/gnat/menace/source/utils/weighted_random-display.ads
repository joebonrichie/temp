--  Given an instance of generic Weighted_Random, this generic procedure will
--  display the relevant content of the specified Generator.  Specifically, it
--  will print the individual weights and the total weight.

generic
procedure Weighted_Random.Display (This : in Generator);